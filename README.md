# Everything you need to Package your C#/.NET project on Nix

Basically, copy the flake.nix, package.nix and updateDeps.sh to your project, edit the two nix files to work with your project, and if needed run updateDeps.sh.
